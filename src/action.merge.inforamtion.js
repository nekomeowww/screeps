//Action of Finding creep set from config file.
var configRoom = require('config.room');
var configSpawn = require('config.spawn');
var functionRoomProbe = require('function.room.probe');

var actionIntergrator = {
    run: function() {

    }
}
